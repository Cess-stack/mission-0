// This is the main app component. It serves as the root of the React component tree
// and is responsible for rendering the primary page of the application,
// which in this case is the NZMarketingAssociationPage.

import React from "react";
import NZMarketingAssociationPage from "./NZMarketingAssociationPage";

function App() {
  return <NZMarketingAssociationPage />;
}

export default App;
